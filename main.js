// No funciona así. Tenemos q importar el archivo con la extensión js
// const data = require('./data.json');

// const body = document.getElementsByTagName('body')[0];

// body.textContent = JSON.stringify(data);